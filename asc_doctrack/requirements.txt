# Core Django
Django==4.2.*
djangorestframework==3.15.*
djangorestframework-simplejwt==5.3.*
django-cors-headers==4.3.*
python-dotenv==1.0.*

# Real-time WebSockets
channels==4.1.*
channels-redis==4.2.*
daphne==4.1.*          # ASGI server (replaces gunicorn for WS support)

# Background tasks
celery==5.3.*
redis==5.0.*

# AI classification & scanning
requests==2.31.*
Pillow==10.3.*         # image processing
scikit-learn==1.4.*    # TF-IDF + Logistic Regression document classifier
huggingface_hub>=0.23  # HuggingFace Inference API (vision-language scanning)

# Firebase FCM v1 (push notifications)
google-auth>=2.28      # OAuth2 service account credentials for FCM HTTP v1

# Database
psycopg2-binary==2.9.* # PostgreSQL (production)
dj-database-url==2.1.* # parse DATABASE_URL env var

# File handling
PyMuPDF==1.24.*        # extract text from PDF uploads (fitz)

# Utilities
gunicorn==21.2.*       # HTTP server
whitenoise==6.7.*      # serve static files efficiently
qrcode[pil]==7.4.*     # QR code generation for tracking numbers
